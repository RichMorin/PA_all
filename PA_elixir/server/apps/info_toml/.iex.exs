# .iex.exs

import InfoToml
alias  InfoToml.{
  Application, CheckItem, CheckTree, IexFun, IndexTree, LoadTree,
  Parser, Reffer, Schemer, Server, Tagger
}

test_path = "Catalog/Hardware/Anova_PC/main.toml"
