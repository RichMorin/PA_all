# cat_sof|tmux/make.toml

[ meta ]

[ os.arch ]

  package     = 'arch_pkgs|community/x86_64/tmux'
  upstream    = 'http://tmux.github.io'
  wiki        = 'arch_wiki|tmux'

[ os.debian ]

  package     = 'debi_pkgs|unstable/tmux'
  search      = 'debi_pkgs|search?keywords=tmux'
  track       = 'https://tracker.debian.org/pkg/tmux'

  commands    = 'sudo apt-get install tmux'
  issues      = 'installs automagically'
  origin      = 'APT'
  status      = 'ok'
